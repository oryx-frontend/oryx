import { ContentComponentSchema } from '@oryx-frontend/experience';
import { IconTypes } from '@oryx-frontend/ui/icon';

export const cartRemoveComponentSchema: ContentComponentSchema = {
  name: 'Remove Cart',
  group: 'Cart',
  icon: IconTypes.Trash,
};
