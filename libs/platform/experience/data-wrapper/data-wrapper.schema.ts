import { ContentComponentSchema } from '@oryx-frontend/experience';
import { DataWrapperComponent } from './data-wrapper.component';

export const dataWrapperSchema: ContentComponentSchema<DataWrapperComponent> = {
  name: 'Data wrapper',
  group: 'Experience',
};
